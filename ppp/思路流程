先描述一下整个开发的流
程吧，第一点呢就是想要通过这个持仓股数来计算20251212当天的持仓市值的话，大概思路就是先获取当天的收盘价，然后用持仓的股数乘以每支股票的收盘价，然后
进行加和就是最终的持仓市值；第二点呢就是先对这个20251212csv进行数据处理，我是这样处理的，先清洗重复值和空值，有重复值的话就对相同的一只股票进行股数
的加和，空值就直接剔除，之后就是把code填充到六位以及补充每个交易所相对应的后缀，这就会形成一只股票一个完整的股票代码；然后第三步就是需要通过tushare
拉取当天的所有股票明细，然后拉取出code，close等等列，也形成一个dataframe，然后对持仓中的股票和全量数据按照code相同的进行合并hold_vol和close两列，
合并之后呢需要查看有无缺失值，因为tushare拉取的全量数据里面不包括停牌退市的股票，这时候就需要对缺失的股票拉取前几个月的数据然后安装日期进行排序，获
取停牌日当天的收盘价，通过这个去计算停牌或者退市的持仓的股票，然后这时候最终就可以计算总的持仓市值了。这是第一个问题，第二个问题的话。需要用到爬虫，
这时候我发现在上证300指数下载的页面中，在点击下载按钮后，相对应的url里面并没有返回任何数据，这时候可能他就对应的下载的url，我直接通过这个url直接把
数据存到一个dataframe中，然后整理出其中包含的300只股票的code，与清洗好的持仓code进行合并，最后一共是包含了121只股票然后之后就是与问题一一样的流程
进行操作最后算出最终的持仓市值，过程中要记得填充缺失的close数据。问题三呢其实这个当日的总收益额有多种计算路径，第一个就是通过当天返回数据中的preclose字段
去计算，公式就是对每支股票求当天收盘价减去昨天收盘价乘以持仓股数最后进行加和，第二个就是通过change字段直接进行计算，第三就是用preclose算出昨日总的持仓市值，
用今日持仓总市值减去昨日持仓总市值。然后计算当日收益率就是直接用算出来的收益额除以昨日总的持仓市值计算，计算与沪深300的超额收益的话就是先用request接口
直接拉取数据获取，再用计算出来的当日收益率减去沪深300收益率就可以了。这就是整个流程



简单介绍架构就是，其中包含多个类config.py文件是统一的token，当天日期和往日搜索开始日期，以及20251212的路径，是整个流程的配置文件，在这一处做改动就可以，   
crawler_simple.py这个文件是我编写的爬虫文件其中包含了等待重新拉取机制，如果第一次没有拉取到的话，会等待几秒重新拉取如果没有网络的话会返回请及时检查网络,,
positionfile.py这个类是转门用于处理20251212.csv文件的，它其中包含了查重整合，查缺失值，补充code至六位以及添加后缀的功能,quotesource.py这个文件呢是tusha
re自动拉取当日数据，填充缺失值，以及拉取沪深300当日收益率，这里面只都只用使用同一个pro对象，不用重复创建,portfolio.py这个是其中包括了整合两个表的方法可以直
接调用，其中也直接包含了计算各种数据的方法，不用在主文件中重复写，只需要调用类方法就可以了，run1.py这是问题一,run2.py问题二,run3.py问题三。


使用流程，第一步在config.py中填入tushare的的token，在这里也可以直接更换日期，但是当日日期必须得是交易日，如若不是交易日，则会多次触发close补充机制会触发tushare频率限制
第二步就是直接运行run1，run2，run3查看输出。
